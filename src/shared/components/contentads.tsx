import React from 'react';

interface ContentAdsProps {
  page?: string;
  position?: 'top' | 'middle' | 'bottom';
}

export const ContentAds: React.FC<ContentAdsProps> = ({  }) => {
  // Ads removed - return null to hide completely
  return null;
};